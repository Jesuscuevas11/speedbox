{% doc %}
  Product Information Content

  Renders the product information layout with media gallery and product details.
  Used by both product-information and featured-product-information sections.

  @param {number} product_media_size - Number of media items in the product
  @param {string} section_id - Unique section ID
  @param {object} settings - Section settings object
  @param {string} media_gallery - Captured HTML for the product media gallery block
  @param {string} product_details - Captured HTML for the product details block
  @param {string} additional_blocks - Captured HTML for additional section blocks

  @example
  {% capture media_gallery %}
    {% content_for 'block', type: '_product-media-gallery', id: 'media-gallery', closest.product: product %}
  {% endcapture %}

  {% capture product_details %}
    {% content_for 'block', type: '_product-details', id: 'product-details', closest.product: product %}
  {% endcapture %}

  {% capture additional_blocks %}
    {% content_for 'blocks' %}
  {% endcapture %}

  {% render 'product-information-content',
    product_media_size: product.media.size,
    section_id: section.id,
    settings: section.settings,
    media_gallery: media_gallery,
    product_details: product_details,
    additional_blocks: additional_blocks
  %}
{% enddoc %}

{% liquid
  # Determine render order based on media position
  if settings.desktop_media_position == 'left'
    assign render_order = 'media,product-details'
  else
    assign render_order = 'product-details,media'
  endif

  # Check if product has media
  assign product_has_media = true
  if product_media_size == 0
    assign product_has_media = false
  endif

  # Build product grid class
  assign product_grid_class = 'product-information__grid'

  if product_has_media == false
    assign product_grid_class = product_grid_class | append: ' product-information--media-none'
  elsif settings.desktop_media_position == 'right'
    assign product_grid_class = product_grid_class | append: ' product-information--media-right'
  else
    assign product_grid_class = product_grid_class | append: ' product-information--media-left'
  endif

  if settings.equal_columns
    assign product_grid_class = product_grid_class | append: ' product-information__grid--half'

    if settings.limit_details_width
      assign product_grid_class = product_grid_class | append: ' product-information__grid--limit-details'
    endif
  endif

  # Determine content width
  case settings.content_width
    when 'content-center-aligned'
      assign content_width = 'page-width'
    when 'content-full-width'
      assign content_width = 'full-width'
  endcase
%}

<div class="section-background color-{{ settings.color_scheme }}"></div>
<div
  class="product-information section section--{{ content_width }} spacing-style color-{{ settings.color_scheme }} relative"
  style="{% render 'spacing-style', settings: settings %} --gap: {{ settings.gap }}px;"
  data-testid="product-information"
>
  {% if settings.desktop_media_position == 'left' and product_has_media and request.page_type == 'product' %}
    {% assign product_href = '#ProductInformation-' | append: section_id %}
    {% render 'skip-to-content-link', href: product_href, text: 'accessibility.skip_to_product_info' %}
  {% endif %}
  <div
    class="{{ product_grid_class }}"
    data-product-grid-content
  >
    {% assign render_order_array = render_order | split: ',' %}
    {% capture media %}
      <div
        class="product-information__media"
        data-testid="product-information-media"
      >
        {{ media_gallery }}
      </div>
    {% endcapture %}

    {% for block in render_order_array %}
      {% if block == 'media' and product_has_media %}
        {{ media }}
      {% elsif block == 'product-details' %}
        {{ product_details }}
      {% endif %}
    {% endfor %}
  </div>

  {{ additional_blocks }}
</div>

{% assign social_proof_id = 'SocialProof-' | append: section_id %}
<section
  id="{{ social_proof_id }}"
  class="pdp-social-proof"
  data-social-proof
  data-product-image="{{ product.featured_image | image_url: width: 400 }}"
>
  <div class="pdp-social-proof__inner">
    <div class="pdp-social-proof__hero">
      <div class="pdp-social-proof__rating-block">
        <div class="pdp-social-proof__stars" aria-hidden="true">
          <span>★</span>
          <span>★</span>
          <span>★</span>
          <span>★</span>
          <span>★</span>
        </div>
        <div class="pdp-social-proof__rating-value">4.9</div>
        <div class="pdp-social-proof__rating-copy">Based on <strong>1,200+ verified reviews</strong></div>
        <a href="https://www.elevaterope.com/pages/reviews" class="pdp-social-proof__link" target="_blank" rel="noopener noreferrer">Read customer reviews</a>
      </div>

      <div class="pdp-social-proof__people">
        <div class="pdp-social-proof__avatars" aria-hidden="true">
          <span>SM</span>
          <span>MR</span>
          <span>AK</span>
          <span>JL</span>
          <span>ES</span>
        </div>
        <div class="pdp-social-proof__people-copy">Join <span>50,000+ athletes</span><br>who trust Elevate</div>
      </div>
    </div>

    <div class="pdp-social-proof__activity-bar">
      <div class="pdp-social-proof__activity-item">
        <svg class="pdp-social-proof__activity-icon" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
          <circle cx="12" cy="12" r="10"></circle>
        </svg>
        <span><strong data-viewer-count>38</strong> people viewing right now</span>
      </div>

      <div class="pdp-social-proof__activity-item">
        <svg class="pdp-social-proof__activity-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
          <polyline points="22 4 12 14.01 9 11.01"></polyline>
        </svg>
        <span>Last order <strong>2 minutes ago</strong></span>
      </div>

      <div class="pdp-social-proof__delivery-pill">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <rect x="1" y="3" width="15" height="13"></rect>
          <path d="M16 8h5l3 3v5h-3"></path>
          <circle cx="5.5" cy="18.5" r="2.5"></circle>
          <circle cx="18.5" cy="18.5" r="2.5"></circle>
        </svg>
        <span>Order in <strong data-countdown>18h 25m</strong> for same-day shipping</span>
      </div>
    </div>

    <div class="pdp-social-proof__gallery-block">
      <div class="pdp-social-proof__gallery-header">
        <h3>Real Customers, Real Results</h3>
        <a href="https://www.elevaterope.com/pages/reviews" class="pdp-social-proof__link" target="_blank" rel="noopener noreferrer">View all reviews</a>
      </div>

      <div class="pdp-social-proof__gallery-grid">
        <button type="button" class="pdp-social-proof__video-card" data-video-id="3U5Bn-R5MLw">
          <img src="https://img.youtube.com/vi/3U5Bn-R5MLw/hqdefault.jpg" alt="Customer review video 1" loading="lazy">
          <span class="pdp-social-proof__video-overlay"><span class="pdp-social-proof__play-button"></span></span>
        </button>
        <button type="button" class="pdp-social-proof__video-card" data-video-id="zX3fB4iTZfU">
          <img src="https://img.youtube.com/vi/zX3fB4iTZfU/hqdefault.jpg" alt="Customer review video 2" loading="lazy">
          <span class="pdp-social-proof__video-overlay"><span class="pdp-social-proof__play-button"></span></span>
        </button>
        <button type="button" class="pdp-social-proof__video-card" data-video-id="ab-TSJ9YS9M">
          <img src="https://img.youtube.com/vi/ab-TSJ9YS9M/hqdefault.jpg" alt="Customer review video 3" loading="lazy">
          <span class="pdp-social-proof__video-overlay"><span class="pdp-social-proof__play-button"></span></span>
        </button>
        <button type="button" class="pdp-social-proof__video-card" data-video-id="jroNtnSdogs">
          <img src="https://img.youtube.com/vi/jroNtnSdogs/hqdefault.jpg" alt="Customer review video 4" loading="lazy">
          <span class="pdp-social-proof__video-overlay"><span class="pdp-social-proof__play-button"></span></span>
        </button>
        <button type="button" class="pdp-social-proof__video-card" data-video-id="N365_szEpaU">
          <img src="https://img.youtube.com/vi/N365_szEpaU/hqdefault.jpg" alt="Customer review video 5" loading="lazy">
          <span class="pdp-social-proof__video-overlay"><span class="pdp-social-proof__play-button"></span></span>
        </button>
        <button type="button" class="pdp-social-proof__video-card" data-video-id="JQchw5EAoWE">
          <img src="https://img.youtube.com/vi/JQchw5EAoWE/hqdefault.jpg" alt="Customer review video 6" loading="lazy">
          <span class="pdp-social-proof__video-overlay"><span class="pdp-social-proof__play-button"></span></span>
        </button>
      </div>
    </div>

    <div class="pdp-social-proof__trust-grid">
      <article class="pdp-social-proof__trust-card">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path></svg>
        <div>
          <h4>90-Day Money Back</h4>
          <p>Try risk-free for 3 months. Not satisfied? Full refund, no questions asked.</p>
        </div>
      </article>
      <article class="pdp-social-proof__trust-card">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><circle cx="12" cy="12" r="10"></circle><path d="M12 6v6l4 2"></path></svg>
        <div>
          <h4>Lifetime Handle Warranty</h4>
          <p>Premium handles guaranteed forever. 90-day warranty on cables and cords.</p>
        </div>
      </article>
      <article class="pdp-social-proof__trust-card">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg>
        <div>
          <h4 data-shipping-title>Free Shipping Over €50</h4>
          <p data-shipping-description>Fast delivery to EU, UK, US, Canada and Australia. Worldwide available.</p>
        </div>
      </article>
    </div>
  </div>

  <div class="pdp-social-proof__notification" data-notification hidden>
    <img src="{{ product.featured_image | image_url: width: 200 }}" alt="{{ product.title | escape }}" data-notification-image>
    <div class="pdp-social-proof__notification-copy">
      <div data-notification-name>Carlos R. from Madrid</div>
      <div data-notification-action>just purchased Athlete Bundle</div>
      <div data-notification-time>2 minutes ago</div>
    </div>
    <button type="button" class="pdp-social-proof__notification-close" data-notification-close aria-label="Close notification">×</button>
  </div>

  <div class="pdp-social-proof__modal" data-video-modal hidden>
    <div class="pdp-social-proof__modal-dialog">
      <button type="button" class="pdp-social-proof__modal-close" data-close-video aria-label="Close video">×</button>
      <iframe data-video-frame src="" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen title="Customer review video"></iframe>
    </div>
  </div>
</section>

<section class="pdp-benefits" data-pdp-benefits>
  <div class="pdp-benefits__inner">
    <header class="pdp-benefits__header">
      <div class="pdp-benefits__badge">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z"></path>
          <path d="M5 3v4"></path>
          <path d="M19 17v4"></path>
          <path d="M3 5h4"></path>
          <path d="M17 19h4"></path>
        </svg>
        Why Choose Elevate Rope
      </div>
      <h2 class="pdp-benefits__title">Transform Your Fitness in<br><span>Half the Time</span></h2>
      <p class="pdp-benefits__subtitle">Stop wasting time and money on ineffective workouts. Get the results you deserve.</p>
    </header>

    <div class="pdp-benefits__grid">
      <article class="pdp-benefits__card">
        <div class="pdp-benefits__card-bg"></div>
        <div class="pdp-benefits__card-content">
          <div class="pdp-benefits__icon pdp-benefits__icon--orange">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z"></path></svg>
          </div>
          <p class="pdp-benefits__label">THE PROBLEM</p>
          <h3 class="pdp-benefits__problem">Spending hours at the gym with minimal results?</h3>
          <div class="pdp-benefits__divider"></div>
          <p class="pdp-benefits__label pdp-benefits__label--solution">THE SOLUTION</p>
          <h4 class="pdp-benefits__solution">Burn 1,000+ calories per hour</h4>
          <p class="pdp-benefits__description">Jump rope torches more calories than running, cycling, or most gym workouts in half the time. Get a full-body cardio workout that delivers real results, fast.</p>
          <div class="pdp-benefits__stat"><span class="pdp-benefits__stat-number pdp-benefits__stat-number--orange">1,000</span><span>calories/hour</span></div>
        </div>
      </article>

      <article class="pdp-benefits__card">
        <div class="pdp-benefits__card-bg"></div>
        <div class="pdp-benefits__card-content">
          <div class="pdp-benefits__icon pdp-benefits__icon--purple">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"></path><circle cx="12" cy="10" r="3"></circle></svg>
          </div>
          <p class="pdp-benefits__label">THE PROBLEM</p>
          <h3 class="pdp-benefits__problem">Tired of commuting to crowded gyms?</h3>
          <div class="pdp-benefits__divider"></div>
          <p class="pdp-benefits__label pdp-benefits__label--solution">THE SOLUTION</p>
          <h4 class="pdp-benefits__solution">Train anywhere, anytime</h4>
          <p class="pdp-benefits__description">No gym membership required. Your jump rope fits in your bag and works anywhere: home, park, hotel room, or backyard. Total fitness freedom in your pocket.</p>
          <div class="pdp-benefits__stat"><span class="pdp-benefits__stat-number pdp-benefits__stat-number--purple">10 mins</span><span>to setup</span></div>
        </div>
      </article>

      <article class="pdp-benefits__card">
        <div class="pdp-benefits__card-bg"></div>
        <div class="pdp-benefits__card-content">
          <div class="pdp-benefits__icon pdp-benefits__icon--green">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M19 7V4a1 1 0 0 0-1-1H5a2 2 0 0 0 0 4h15a1 1 0 0 1 1 1v4h-3a2 2 0 0 0 0 4h3a1 1 0 0 0 1-1v-2a1 1 0 0 0-1-1"></path><path d="M3 5v14a2 2 0 0 0 2 2h15a1 1 0 0 0 1-1v-4"></path></svg>
          </div>
          <p class="pdp-benefits__label">THE PROBLEM</p>
          <h3 class="pdp-benefits__problem">Wasting money on expensive gym memberships?</h3>
          <div class="pdp-benefits__divider"></div>
          <p class="pdp-benefits__label pdp-benefits__label--solution">THE SOLUTION</p>
          <h4 class="pdp-benefits__solution">One-time investment, lifetime results</h4>
          <p class="pdp-benefits__description">Cancel your monthly membership. This is a one-time purchase that pays for itself quickly, with zero recurring fees.</p>
          <div class="pdp-benefits__stat"><span class="pdp-benefits__stat-number pdp-benefits__stat-number--green">EUR 0</span><span>monthly fees</span></div>
        </div>
      </article>
    </div>
  </div>
</section>

<section class="pdp-featured-legacy" data-pdp-featured-legacy>
  <div class="pdp-featured-legacy__inner">
    <div class="pdp-featured-legacy__grid">
      <div class="pdp-featured-legacy__media">
        <a href="/es/products/storm-beaded-jump-rope" aria-label="Storm - Cuerda para saltar con cuentas">
          <img
            src="https://www.elevaterope.com/cdn/shop/files/HEROExample_00de0200-0308-4d7f-84fb-9f7ab9c95a08.jpg?v=1770505158&width=1200"
            alt="Storm - Cuerda para saltar con cuentas"
            loading="lazy"
            width="973"
            height="973"
          >
        </a>
      </div>

      <div class="pdp-featured-legacy__info">
        <h2 class="pdp-featured-legacy__title">Storm - Cuerda para saltar con cuentas</h2>

        <h5><strong>Features:</strong></h5>
        <ul>
          <li>- 3.5MM High Quality Polycord</li>
          <li>- Durable shatterproof beads</li>
          <li>- Rock-hard, Shatter-free Handles</li>
          <li>- Perfect for beginners, freestyle and any skills that require more feedback for better control</li>
        </ul>

        <h5><strong>Benefits:</strong></h5>
        <ul>
          <li>- Eliminates tangling, makes you trip less and allows for smooth transitions and the best jumping experience every time.</li>
          <li>- Highest quality PVC with nylon core allows for a longer lifespan and gives you the freedom to jump anywhere.</li>
          <li>- Rock-hard, shatter-free handles give you the space to practice freestyle tricks on concrete without breaking.</li>
          <li>- 5MM Cable gives you the feedback you need to feel your rope and the speed you want for an effective full-body workout.</li>
        </ul>

        <a href="/es/products/storm-beaded-jump-rope" class="pdp-featured-legacy__link">
          Ver detalles completos
        </a>
      </div>
    </div>
  </div>
</section>

<section class="pdp-grid-banner" data-pdp-grid-banner>
  <div class="pdp-grid-banner__inner">
    <div class="pdp-grid-banner__grid">
      <article class="pdp-grid-banner__card pdp-grid-banner__card--text">
        <h4 class="pdp-grid-banner__kicker">INCLUYE :</h4>
        <h5 class="pdp-grid-banner__headline">1X CUERDA CON PERLAS</h5>
        <p class="pdp-grid-banner__description">
          Las cuerdas con cuentas son perfectas para quienes buscan practicar trucos, envolturas de estilo libre,
          liberaciones y explorar el mundo del salto de cuerda estilo libre.
        </p>
      </article>

      <article class="pdp-grid-banner__card pdp-grid-banner__card--image">
        <img
          src="https://www.elevaterope.com/cdn/shop/files/Copy-of-Beaded-Jump-Rope-Lynx.png?v=1745700849&width=1500"
          alt="Lynx - Beaded Jump Rope - Elevate Rope"
          loading="lazy"
          width="1250"
          height="1250"
        >
      </article>

      <article class="pdp-grid-banner__card pdp-grid-banner__card--image">
        <img
          src="https://www.elevaterope.com/cdn/shop/files/speed_rope_comparison_image.webp?v=1742608446&width=1500"
          alt="Comparacion de cuerdas"
          loading="lazy"
          width="1650"
          height="1250"
        >
      </article>
    </div>
  </div>
</section>

<section class="pdp-multicolumn" data-pdp-multicolumn>
  <div class="pdp-multicolumn__inner">
    <header class="pdp-multicolumn__header">
      <h2>¿Por qué una cuerda de cuentas?</h2>
      <p>
        Cada tipo de cuerda tiene sus propios beneficios y se adapta a un estilo diferente de saltar. La estructura
        de una cuerda con cuentas consiste en un cordón de poliéster de 3.0MM envuelto en cuentas duraderas y coloridas.
      </p>
    </header>

    <div class="pdp-multicolumn__list" role="list">
      <article class="pdp-multicolumn__item" role="listitem">
        <div class="pdp-multicolumn__media">
          <img
            src="https://www.elevaterope.com/cdn/shop/files/5_565de955-400d-40e3-9966-efdee3ec3025.png?v=1743957331&width=1420"
            alt="Retroalimentacion visual de cuerda con cuentas"
            loading="lazy"
            width="1920"
            height="1080"
          >
        </div>
        <div class="pdp-multicolumn__info">
          <h6>Las cuentas atrapan aire, lo que resulta en más retroalimentación y ayuda a sentir mejor la ubicación de la cuerda.</h6>
        </div>
      </article>

      <article class="pdp-multicolumn__item" role="listitem">
        <div class="pdp-multicolumn__media">
          <img
            src="https://www.elevaterope.com/cdn/shop/files/4_b45ab812-0834-47b0-994c-7c366b30e1c3.png?v=1743957332&width=1420"
            alt="Control con cuerda de cuentas"
            loading="lazy"
            width="1920"
            height="1080"
          >
        </div>
        <div class="pdp-multicolumn__info">
          <h6>Una cuerda con cuentas ha demostrado ser perfecta para principiantes, estilo libre y cualquier habilidad que requiera más retroalimentación para un mejor control.</h6>
        </div>
      </article>
    </div>

    <div class="pdp-multicolumn__dots" data-multi-dots aria-label="Paginacion del carrusel"></div>
  </div>
</section>

{% stylesheet %}
  .product-information {
    gap: var(--gap) 0;
  }

  /* Base grid layout */
  .product-information__grid {
    display: grid;
    grid-template-columns: subgrid;
    grid-column: 1 / -1;
  }

  /* Default column positions */
  .product-details {
    order: 1;
  }

  .product-information__media {
    order: 0;
    width: 0;
    min-width: 100%;
  }

  .product-information__media .product-media-gallery__placeholder-image {
    width: 100%;
    height: 100%;
  }

  /* Mobile styles */
  @media screen and (max-width: 749px) {
    .product-information__media {
      grid-column: 1 / -1;
    }

    .product-details {
      grid-column: 2 / 3;
    }
  }

  /* Desktop styles */
  @media screen and (min-width: 750px) {
    .product-information__grid {
      grid-column: 2;
    }

    /* Position when there is no media */
    .product-information__grid.product-information--media-none {
      .product-details {
        width: var(--narrow-content-width);
        margin: 0 auto;
      }
    }

    /* Position when there is media */
    .product-information__grid:not(.product-information--media-none) {
      /* Media on the left side */
      &.product-information--media-left {
        grid-template-columns: 1fr min(50vw, var(--sidebar-width));

        .product-information__media {
          padding-right: calc(var(--gap, 0) / 2);
        }

        .product-details {
          padding-left: calc(var(--gap, 0) / 2);
        }

        &:has(.media-gallery--extend) {
          grid-column: 1 / 3;
        }
      }

      /* Media on the right side */
      &.product-information--media-right {
        grid-template-columns: min(50vw, var(--sidebar-width)) 1fr;

        .product-information__media {
          padding-left: calc(var(--gap, 0) / 2);
          order: 1;
        }

        .product-details {
          padding-right: calc(var(--gap, 0) / 2);
          order: 0;
        }

        &:has(.media-gallery--extend) {
          grid-column: 2 / -1;
        }
      }

      /* Equal width columns */
      &.product-information__grid--half,
      &.product-information__grid--half:has(.media-gallery--extend) {
        grid-column: 1 / -1;
        grid-template-columns:
          var(--full-page-grid-margin) calc(var(--full-page-grid-central-column-width) / 2) calc(
            var(--full-page-grid-central-column-width) / 2
          )
          var(--full-page-grid-margin);

        &.product-information--media-left {
          .product-information__media {
            grid-column: 2 / 3;

            &:has(.media-gallery--extend) {
              grid-column: 1 / 3;
            }
          }

          .product-details {
            grid-column: 3 / 4;
          }
        }

        &.product-information--media-right {
          .product-information__media {
            grid-column: 3 / 4;

            &:has(.media-gallery--extend) {
              grid-column: 3 / -1;
            }
          }

          .product-details {
            grid-column: 2 / 3;
          }
        }
      }
    }

    /* Handle full width section */
    .section--full-width {
      .product-information__grid:not(.product-information--media-none) {
        &.product-information--media-left,
        &.product-information--media-right {
          grid-column: 1 / -1;
        }

        &.product-information--media-left .product-details {
          padding-inline-end: var(--padding-lg);
        }

        &.product-information--media-right .product-details {
          padding-inline-start: var(--padding-lg);
        }

        &.product-information__grid--half.product-information--media-left {
          .product-information__media {
            grid-column: 1 / 3;
          }

          .product-details {
            grid-column: 3 / -1;
          }
        }

        &.product-information__grid--half.product-information--media-right {
          .product-information__media {
            grid-column: 3 / -1;
          }

          .product-details {
            grid-column: 1 / 3;
          }
        }
      }
    }
  }

  /* Wider sidebar for large screens */
  @media screen and (min-width: 1200px) {
    .product-information__grid:not(
        .product-information__grid--half,
        .product-information--media-none
      ).product-information--media-left {
      grid-template-columns: 2fr 1fr;
    }

    .product-information__grid:not(
        .product-information__grid--half,
        .product-information--media-none
      ).product-information--media-right {
      grid-template-columns: 1fr 2fr;
    }
  }

  .product-information__grid--limit-details .product-details > .group-block {
    --details-max-width: var(--sidebar-width);
    max-width: var(--details-max-width);
  }

  @media screen and (min-width: 1600px) {
    .product-information__grid--limit-details .product-details > .group-block {
      --details-max-width: 32rem;
    }
  }

  @media screen and (min-width: 750px) {
    .product-information .product-details > .group-block {
      transform: translateX(-100px);
    }
  }

  .product-details .text-block:is(.h1, .h2, .h3, .h4, .h5, .h6) > h1 {
    box-sizing: border-box;
    color: rgb(10, 10, 10);
    display: block;
    font-family: 'Instrument Serif', Georgia, serif;
    font-size: 36px;
    font-style: normal;
    font-weight: 400;
    line-height: 39.6px;
    list-style-image: none;
    list-style-position: outside;
    list-style-type: none;
    margin-block-start: 0;
    margin-block-end: 10px;
    margin-top: 0;
    margin-right: 0;
    margin-bottom: 10px;
    margin-left: 0;
    padding-top: 0;
    padding-right: 0;
    padding-bottom: 0;
    padding-left: 0;
    text-transform: none;
    unicode-bidi: isolate;
    word-break: break-word;
    -webkit-font-smoothing: antialiased;
  }

  @media screen and (min-width: 750px) {
    .product-details .text-block:is(.h1, .h2, .h3, .h4, .h5, .h6) > h1 {
      white-space: nowrap;
      text-wrap: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
  }

  @media screen and (max-width: 749px) {
    .product-information .media-gallery--carousel {
      --padding-inline-start: 12px !important;
      --padding-inline-end: 12px !important;
    }

    .product-information .media-gallery--carousel slideshow-component {
      --carousel-mobile-slide-size: 267.16px;
      --carousel-mobile-width-first: calc((100% - var(--carousel-mobile-slide-size)) * 0.9 + var(--carousel-mobile-slide-size));
      --carousel-mobile-width-middle: calc((100% - var(--carousel-mobile-slide-size)) * 0.6324 + var(--carousel-mobile-slide-size));
      --carousel-mobile-width-current: var(--carousel-mobile-width-first);
    }

    .product-information .media-gallery--carousel slideshow-component:has(
        slideshow-slide:not(:first-child):not(:last-child)[aria-hidden='false']
      ) {
      --carousel-mobile-width-current: var(--carousel-mobile-width-middle);
    }

    .product-information .media-gallery--carousel slideshow-controls.media-gallery__mobile-controls {
      position: static;
      width: var(--carousel-mobile-width-current);
      max-width: 100%;
      margin-inline: auto;
      margin-top: 30px;
      justify-content: center;
    }

    .product-information .media-gallery--carousel slideshow-controls.media-gallery__mobile-controls .slideshow-controls__counter {
      background-color: transparent;
      backdrop-filter: none;
      color: rgb(128 128 128);
      padding-inline: 0;
      margin-inline: 0;
    }

    .product-information
      .media-gallery--carousel
      slideshow-controls.media-gallery__mobile-controls
      .slideshow-controls__counter
      .slash {
      color: rgb(128 128 128);
    }

    .product-information .media-gallery--carousel slideshow-container {
      margin-top: 12px;
      width: var(--carousel-mobile-width-current);
      max-width: 100%;
      margin-inline: auto;
      overflow: hidden;
    }

    .product-information .media-gallery--carousel slideshow-slides {
      --slideshow-gap: 15px;
      padding-inline: max(0px, calc((100% - var(--carousel-mobile-slide-size)) / 2));
      scroll-padding-inline: max(0px, calc((100% - var(--carousel-mobile-slide-size)) / 2));
    }

    .product-information .media-gallery--carousel :not(.dialog-zoomed-gallery) > .product-media-container {
      width: var(--carousel-mobile-slide-size);
      min-width: var(--carousel-mobile-slide-size);
      max-width: var(--carousel-mobile-slide-size);
      height: var(--carousel-mobile-slide-size);
      aspect-ratio: 1 / 1;
    }

    .product-information .media-gallery--carousel :not(.dialog-zoomed-gallery) > .product-media-container .product-media {
      width: 100%;
      height: 100%;
    }

    .product-information .media-gallery--carousel :not(.dialog-zoomed-gallery) > .product-media-container :is(img, .deferred-media__poster-image) {
      width: 100%;
      height: 100%;
      object-fit: contain;
      object-position: center;
    }

    .product-details .text-block:is(.h1, .h2, .h3, .h4, .h5, .h6) > h1 {
      white-space: normal;
      text-wrap: wrap;
      overflow: visible;
      text-overflow: clip;
    }
  }

  @media screen and (min-width: 750px) {
    .product-information .media-gallery--carousel slideshow-container {
      width: 495.2px;
      max-width: 100%;
      overflow: hidden;
    }

    .product-information .media-gallery--carousel slideshow-component:has(slideshow-controls[thumbnails]) {
      width: 685.4px;
      max-width: 100%;
      overflow: hidden;
    }

    .product-information .media-gallery--carousel slideshow-controls[thumbnails] {
      box-sizing: border-box;
      color: rgb(45, 45, 45);
      display: block;
      flex-shrink: 0;
      font-family: Inter, sans-serif;
      font-size: 14px;
      font-style: normal;
      font-weight: 300;
      height: 134px;
      line-height: 25.2px;
      list-style-image: none;
      list-style-position: outside;
      list-style-type: none;
      margin-top: 12px;
      margin-right: 0;
      margin-bottom: 0;
      margin-left: 0;
      order: 2;
      overflow-x: hidden;
      overflow-y: hidden;
      padding-top: 0;
      padding-right: 0;
      padding-bottom: 0;
      padding-left: 0;
      position: relative;
      unicode-bidi: isolate;
      width: 685.4px;
      max-width: 100%;
      -webkit-font-smoothing: antialiased;
    }

    .product-information .media-gallery--carousel slideshow-controls[thumbnails] .slideshow-controls__thumbnails-container {
      overflow-x: hidden;
      overflow-y: hidden;
      width: 100%;
      max-width: 100%;
    }

    .product-information .media-gallery--carousel slideshow-controls[thumbnails] .slideshow-controls__thumbnails {
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .product-information .media-gallery--carousel slideshow-controls[thumbnails] .slideshow-controls__thumbnail {
      --aspect-ratio: 1 / 1;
      width: 122px;
      min-width: 122px;
      max-width: 122px;
      height: 122px;
      border-radius: 8px;
      overflow: hidden;
      border: 1px solid transparent;
      padding: 0;
      box-sizing: border-box;
      opacity: 0.55;
      transition: opacity 0.2s ease, border-color 0.2s ease;
    }

    .product-information .media-gallery--carousel slideshow-controls[thumbnails] .slideshow-controls__thumbnail img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: 8px;
      border: 0;
      outline: 0;
    }

    .product-information .media-gallery--carousel slideshow-controls[thumbnails] .slideshow-controls__thumbnail[aria-selected='true'] {
      border: 2px solid #5510dd;
      opacity: 1;
      outline: none;
      box-shadow: none;
    }

    .product-information .media-gallery--carousel slideshow-controls[thumbnails] .slideshow-controls__thumbnail:focus-visible {
      outline: none;
      box-shadow: none;
    }

    .product-information .media-gallery--carousel :not(.dialog-zoomed-gallery) > .product-media-container {
      width: 100%;
      min-width: 100%;
      max-width: 100%;
      height: 495.2px;
      aspect-ratio: 1 / 1;
    }

    .product-information .media-gallery--carousel :not(.dialog-zoomed-gallery) > .product-media-container .product-media {
      width: 100%;
      height: 100%;
    }

    .product-information .media-gallery--carousel :not(.dialog-zoomed-gallery) > .product-media-container :is(img, .deferred-media__poster-image) {
      width: 100%;
      height: 100%;
      object-fit: contain;
      object-position: center;
    }
  }

  @media screen and (min-width: 750px) {
    .product-details product-price [ref='priceContainer'] {
      display: flex;
      align-items: flex-end;
      gap: 8px;
      flex-wrap: wrap;
    }

    .product-details product-price [ref='priceContainer'] > [role='group'] {
      display: flex;
      align-items: flex-end;
    }

    .product-details product-price .price {
      box-sizing: border-box;
      color: rgb(10, 10, 10);
      display: block;
      font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
      font-size: 42px;
      font-style: normal;
      font-weight: 700;
      height: 42px;
      letter-spacing: -1.5px;
      line-height: 42px;
      list-style-image: none;
      list-style-position: outside;
      list-style-type: none;
      margin-top: 0;
      margin-right: 0;
      margin-bottom: 0;
      margin-left: 0;
      padding-top: 0;
      padding-right: 0;
      padding-bottom: 0;
      padding-left: 0;
      width: 119.463px;
      -webkit-font-smoothing: antialiased;
    }

    .product-details product-price .compare-at-price {
      box-sizing: border-box;
      color: rgb(212, 212, 212);
      opacity: 1;
      display: block;
      font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
      font-size: 18px;
      font-style: normal;
      font-weight: 300;
      height: 27px;
      line-height: 27px;
      list-style-image: none;
      list-style-position: outside;
      list-style-type: none;
      margin-top: 0;
      margin-right: 0;
      margin-bottom: 0;
      margin-left: 0;
      padding-top: 0;
      padding-right: 0;
      padding-bottom: 0;
      padding-left: 0;
      text-decoration-color: rgb(212, 212, 212);
      text-decoration-line: line-through;
      text-decoration-style: solid;
      text-decoration-thickness: auto;
      width: 65.075px;
      -webkit-font-smoothing: antialiased;
    }

    .product-details product-price .price-savings {
      background-attachment: scroll;
      background-clip: border-box;
      background-color: rgb(85, 16, 221);
      background-image: none;
      background-origin: padding-box;
      background-position-x: 0%;
      background-position-y: 0%;
      background-repeat: repeat;
      background-size: auto;
      border-top-left-radius: 6px;
      border-top-right-radius: 6px;
      border-bottom-left-radius: 6px;
      border-bottom-right-radius: 6px;
      box-sizing: border-box;
      color: rgb(255, 255, 255);
      display: block;
      font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
      font-size: 12px;
      font-style: normal;
      font-weight: 700;
      height: 24px;
      line-height: 12px;
      list-style-image: none;
      list-style-position: outside;
      list-style-type: none;
      margin-top: 0;
      margin-right: 0;
      margin-bottom: 0;
      margin-left: 0;
      padding-top: 6px;
      padding-right: 12px;
      padding-bottom: 6px;
      padding-left: 12px;
      width: 79.3875px;
      -webkit-font-smoothing: antialiased;
    }
  }

  @media screen and (max-width: 749px) {
    .product-details product-price [ref='priceContainer'] {
      display: flex;
      align-items: flex-end;
      gap: 8px;
      flex-wrap: wrap;
    }

    .product-details product-price [ref='priceContainer'] > [role='group'] {
      display: flex;
      align-items: flex-end;
    }

    .product-details product-price .price {
      box-sizing: border-box;
      color: rgb(10, 10, 10);
      cursor: pointer;
      display: block;
      font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
      font-size: 38px;
      font-style: normal;
      font-weight: 700;
      height: 38px;
      letter-spacing: -1.5px;
      line-height: 38px;
      list-style-image: none;
      list-style-position: outside;
      list-style-type: none;
      margin-top: 0;
      margin-right: 0;
      margin-bottom: 0;
      margin-left: 0;
      padding-top: 0;
      padding-right: 0;
      padding-bottom: 0;
      padding-left: 0;
      scrollbar-width: none;
      width: 107.229px;
      -webkit-font-smoothing: antialiased;
    }

    .product-details product-price .compare-at-price {
      box-sizing: border-box;
      color: rgb(212, 212, 212);
      opacity: 1;
      cursor: pointer;
      display: block;
      font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
      font-size: 18px;
      font-style: normal;
      font-weight: 300;
      height: 27px;
      line-height: 27px;
      list-style-image: none;
      list-style-position: outside;
      list-style-type: none;
      margin-top: 0;
      margin-right: 0;
      margin-bottom: 0;
      margin-left: 0;
      padding-top: 0;
      padding-right: 0;
      padding-bottom: 0;
      padding-left: 0;
      scrollbar-width: none;
      text-decoration-color: rgb(212, 212, 212);
      text-decoration-line: line-through;
      text-decoration-style: solid;
      text-decoration-thickness: auto;
      width: 65.0729px;
      -webkit-font-smoothing: antialiased;
    }

    .product-details product-price .price-savings {
      background-attachment: scroll;
      background-clip: border-box;
      background-color: rgb(85, 16, 221);
      background-image: none;
      background-origin: padding-box;
      background-position-x: 0%;
      background-position-y: 0%;
      background-repeat: repeat;
      background-size: auto;
      border-top-left-radius: 6px;
      border-top-right-radius: 6px;
      border-bottom-left-radius: 6px;
      border-bottom-right-radius: 6px;
      box-sizing: border-box;
      color: rgb(255, 255, 255);
      display: block;
      font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
      font-size: 12px;
      font-style: normal;
      font-weight: 700;
      height: 24px;
      line-height: 12px;
      list-style-image: none;
      list-style-position: outside;
      list-style-type: none;
      margin-top: 0;
      margin-right: 0;
      margin-bottom: 0;
      margin-left: 0;
      padding-top: 6px;
      padding-right: 12px;
      padding-bottom: 6px;
      padding-left: 12px;
      width: 79.3875px;
      -webkit-font-smoothing: antialiased;
    }
  }

  /* If the header is sticky, make product details content stick underneath the header */
  body:has(#header-group #header-component[data-sticky-state='active']) .product-details.sticky-content--desktop {
    --sticky-header-offset: var(--header-height);
  }

  .pdp-social-proof {
    box-sizing: border-box;
    color: rgb(45, 45, 45);
    display: block;
    font-family: Inter, sans-serif;
    font-size: 14px;
    font-style: normal;
    font-weight: 300;
    line-height: 25.2px;
    margin: 0;
    padding: 0;
    unicode-bidi: isolate;
    width: 100%;
    -webkit-font-smoothing: antialiased;
  }

  .pdp-social-proof__inner {
    width: calc(100% - 320px);
    max-width: 1320.8px;
    margin: 0 auto;
    background: #fff;
    border-bottom: 1px solid #e5e5e5;
    padding: 40px 0 32px;
  }

  .pdp-social-proof__hero {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 48px;
    flex-wrap: wrap;
    margin-bottom: 32px;
  }

  .pdp-social-proof__rating-block {
    display: flex;
    flex-direction: column;
    gap: 8px;
    min-width: 300px;
  }

  .pdp-social-proof__stars {
    display: flex;
    gap: 4px;
    color: #5510dd;
    font-size: 32px;
    line-height: 1;
  }

  .pdp-social-proof__rating-value {
    font-size: 56px;
    font-weight: 800;
    color: #1a1a1a;
    line-height: 1;
    letter-spacing: -1px;
  }

  .pdp-social-proof__rating-copy {
    color: #666;
    font-size: 16px;
    font-weight: 500;
  }

  .pdp-social-proof__rating-copy strong,
  .pdp-social-proof__people-copy,
  .pdp-social-proof__trust-card h4,
  .pdp-social-proof__activity-item strong {
    color: #1a1a1a;
  }

  .pdp-social-proof__link {
    width: fit-content;
    color: #5510dd;
    font-size: 14px;
    font-weight: 600;
    text-decoration: none;
    border-bottom: 2px solid #5510dd;
    padding-bottom: 2px;
  }

  .pdp-social-proof__people {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .pdp-social-proof__avatars {
    display: flex;
    padding-left: 12px;
  }

  .pdp-social-proof__avatars span {
    width: 48px;
    height: 48px;
    margin-left: -12px;
    border-radius: 999px;
    border: 3px solid #fff;
    background: linear-gradient(135deg, #5510dd 0%, #7c3aed 100%);
    color: #fff;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    font-weight: 700;
  }

  .pdp-social-proof__people-copy {
    font-size: 15px;
    font-weight: 600;
    line-height: 1.4;
  }

  .pdp-social-proof__people-copy span,
  .pdp-social-proof__activity-item strong,
  .pdp-social-proof__delivery-pill strong {
    color: #5510dd;
  }

  .pdp-social-proof__activity-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    flex-wrap: wrap;
    margin-bottom: 24px;
    padding: 16px 20px;
    border: 1px solid #e8dbff;
    border-radius: 12px;
    background: linear-gradient(135deg, #f8f5ff 0%, #faf8ff 100%);
  }

  .pdp-social-proof__activity-item {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 14px;
    font-weight: 600;
  }

  .pdp-social-proof__activity-icon {
    width: 20px;
    height: 20px;
    color: #5510dd;
    animation: pdpSocialProofPulse 2s ease-in-out infinite;
  }

  @keyframes pdpSocialProofPulse {
    0%,
    100% {
      opacity: 1;
      transform: scale(1);
    }

    50% {
      opacity: 0.7;
      transform: scale(0.95);
    }
  }

  .pdp-social-proof__delivery-pill {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 12px 20px;
    border-radius: 8px;
    background: #1a1a1a;
    color: #fff;
    font-size: 14px;
    font-weight: 600;
  }

  .pdp-social-proof__delivery-pill svg {
    width: 20px;
    height: 20px;
    flex-shrink: 0;
  }

  .pdp-social-proof__delivery-pill strong {
    background: #fff;
    padding: 2px 8px;
    border-radius: 4px;
    font-variant-numeric: tabular-nums;
  }

  .pdp-social-proof__gallery-block {
    margin-bottom: 32px;
  }

  .pdp-social-proof__gallery-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    margin-bottom: 20px;
  }

  .pdp-social-proof__gallery-header h3 {
    margin: 0;
    font-size: 18px;
    font-weight: 700;
    color: #1a1a1a;
  }

  .pdp-social-proof__gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 16px;
  }

  .pdp-social-proof__video-card {
    position: relative;
    padding: 0;
    border: 0;
    border-radius: 12px;
    overflow: hidden;
    cursor: pointer;
    aspect-ratio: 16 / 9;
    background: #f0f0f0;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .pdp-social-proof__video-card:hover {
    transform: scale(1.03);
    box-shadow: 0 8px 24px rgba(85, 16, 221, 0.2);
  }

  .pdp-social-proof__video-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .pdp-social-proof__video-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 0, 0, 0.3);
  }

  .pdp-social-proof__play-button {
    width: 56px;
    height: 56px;
    border-radius: 999px;
    background: #5510dd;
    position: relative;
  }

  .pdp-social-proof__play-button::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 52%;
    transform: translate(-50%, -50%);
    border-left: 14px solid #fff;
    border-top: 9px solid transparent;
    border-bottom: 9px solid transparent;
  }

  .pdp-social-proof__trust-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 24px;
    padding-top: 24px;
    border-top: 1px solid #e5e5e5;
  }

  .pdp-social-proof__trust-card {
    display: flex;
    align-items: flex-start;
    gap: 12px;
  }

  .pdp-social-proof__trust-card svg {
    width: 24px;
    height: 24px;
    color: #5510dd;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .pdp-social-proof__trust-card h4 {
    margin: 0 0 4px;
    font-size: 15px;
    font-weight: 700;
  }

  .pdp-social-proof__trust-card p {
    margin: 0;
    font-size: 13px;
    color: #666;
    font-weight: 500;
    line-height: 1.4;
  }

  .pdp-social-proof__notification {
    position: fixed;
    left: 20px;
    bottom: 20px;
    z-index: 30;
    display: flex;
    align-items: center;
    gap: 12px;
    max-width: 320px;
    padding: 16px;
    border: 1px solid #e5e5e5;
    border-radius: 12px;
    background: #fff;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
  }

  .pdp-social-proof__notification[hidden],
  .pdp-social-proof__modal[hidden] {
    display: none;
  }

  .pdp-social-proof__notification img {
    width: 56px;
    height: 56px;
    border-radius: 8px;
    object-fit: contain;
    background: #f9f9f9;
    padding: 4px;
    flex-shrink: 0;
  }

  .pdp-social-proof__notification-copy {
    flex: 1;
    min-width: 0;
  }

  .pdp-social-proof__notification-copy > :first-child {
    font-size: 14px;
    font-weight: 700;
    color: #1a1a1a;
  }

  .pdp-social-proof__notification-copy > :nth-child(2) {
    font-size: 13px;
    color: #666;
    font-weight: 500;
  }

  .pdp-social-proof__notification-copy > :last-child {
    margin-top: 4px;
    font-size: 12px;
    color: #999;
  }

  .pdp-social-proof__notification-close,
  .pdp-social-proof__modal-close {
    border: 0;
    background: none;
    cursor: pointer;
  }

  .pdp-social-proof__notification-close {
    width: 20px;
    height: 20px;
    color: #999;
    font-size: 18px;
    line-height: 1;
  }

  .pdp-social-proof__modal {
    position: fixed;
    inset: 0;
    z-index: 10000;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
    background: rgba(0, 0, 0, 0.9);
  }

  .pdp-social-proof__modal-dialog {
    position: relative;
    width: 100%;
    max-width: 900px;
    aspect-ratio: 16 / 9;
    background: #000;
    border-radius: 12px;
    overflow: hidden;
  }

  .pdp-social-proof__modal-close {
    position: absolute;
    top: -40px;
    right: 0;
    width: 40px;
    height: 40px;
    color: #fff;
    font-size: 32px;
  }

  .pdp-social-proof__modal iframe {
    width: 100%;
    height: 100%;
    border: 0;
  }

  .pdp-benefits {
    box-sizing: border-box;
    color: rgb(45, 45, 45);
    display: block;
    font-family: Inter, sans-serif;
    font-size: 14px;
    font-style: normal;
    font-weight: 300;
    line-height: 25.2px;
    margin: 0;
    padding: 0;
    unicode-bidi: isolate;
    width: 100%;
    -webkit-font-smoothing: antialiased;
  }

  .pdp-benefits__inner {
    width: calc(100% - 320px);
    max-width: 1320.8px;
    margin: 0 auto;
    padding: 64px 0;
    background: linear-gradient(to bottom, #f9fafb, #ffffff);
  }

  .pdp-benefits__header {
    text-align: center;
    margin-bottom: 64px;
  }

  .pdp-benefits__badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: #f3e8ff;
    color: #7c3aed;
    padding: 8px 16px;
    border-radius: 999px;
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 16px;
  }

  .pdp-benefits__badge svg {
    width: 16px;
    height: 16px;
  }

  .pdp-benefits__title {
    margin: 0 0 16px;
    font-size: 40px;
    font-weight: 700;
    line-height: 1.2;
    color: #111827;
  }

  .pdp-benefits__title span {
    background: linear-gradient(to right, #5510dd, #ec4899);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .pdp-benefits__subtitle {
    max-width: 672px;
    margin: 0 auto;
    font-size: 20px;
    color: #6b7280;
  }

  .pdp-benefits__grid {
    display: grid;
    grid-template-columns: 1fr;
    justify-content: center;
    justify-items: center;
    gap: 24px;
  }

  .pdp-benefits__card {
    position: relative;
    width: min(370px, 100%);
    min-height: 349px;
    overflow: hidden;
    border-radius: 16px;
    border: 1px solid #f3f4f6;
    background: #fff;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease;
  }

  .pdp-benefits__card:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
    border-color: #e9d5ff;
  }

  .pdp-benefits__card-bg {
    position: absolute;
    inset: 0;
    opacity: 0;
    transition: opacity 0.3s ease;
    background: linear-gradient(to bottom right, #faf5ff, #fce7f3);
  }

  .pdp-benefits__card:hover .pdp-benefits__card-bg {
    opacity: 1;
  }

  .pdp-benefits__card-content {
    position: relative;
    height: 100%;
    display: flex;
    flex-direction: column;
    z-index: 1;
    padding: 22px;
  }

  .pdp-benefits__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 52px;
    height: 52px;
    border-radius: 12px;
    margin-bottom: 14px;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    color: #fff;
  }

  .pdp-benefits__icon svg {
    width: 26px;
    height: 26px;
  }

  .pdp-benefits__icon--orange { background: linear-gradient(to bottom right, #f97316, #dc2626); }
  .pdp-benefits__icon--purple { background: linear-gradient(to bottom right, #5510dd, #ec4899); }
  .pdp-benefits__icon--green { background: linear-gradient(to bottom right, #10b981, #059669); }

  .pdp-benefits__label {
    margin: 0 0 4px;
    color: #6b7280;
    font-size: 12px;
    font-weight: 500;
  }

  .pdp-benefits__label--solution {
    color: #5510dd;
  }

  .pdp-benefits__problem {
    margin: 0 0 10px;
    font-size: 16px;
    font-weight: 600;
    line-height: 1.35;
    color: #1f2937;
  }

  .pdp-benefits__divider {
    width: 48px;
    height: 4px;
    border-radius: 999px;
    margin: 10px 0;
    background: linear-gradient(to right, #5510dd, #ec4899);
  }

  .pdp-benefits__solution {
    margin: 0 0 8px;
    font-size: 18px;
    font-weight: 700;
    color: #111827;
    line-height: 1.3;
  }

  .pdp-benefits__description {
    margin: 0;
    font-size: 14px;
    color: #6b7280;
    line-height: 1.45;
    display: block;
  }

  .pdp-benefits__stat {
    display: flex;
    align-items: baseline;
    gap: 8px;
    margin-top: auto;
    padding-top: 12px;
    border-top: 1px solid #f3f4f6;
    color: #6b7280;
    font-size: 13px;
    font-weight: 500;
  }

  .pdp-benefits__stat-number {
    font-size: 30px;
    font-weight: 700;
    line-height: 1;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .pdp-benefits__stat-number--orange { background-image: linear-gradient(to right, #f97316, #dc2626); }
  .pdp-benefits__stat-number--purple { background-image: linear-gradient(to right, #5510dd, #ec4899); }
  .pdp-benefits__stat-number--green { background-image: linear-gradient(to right, #10b981, #059669); }

  .pdp-featured-legacy {
    box-sizing: border-box;
    color: rgb(45, 45, 45);
    display: block;
    font-family: Inter, sans-serif;
    font-size: 14px;
    font-style: normal;
    font-weight: 300;
    line-height: 25.2px;
    margin: 0;
    padding: 0;
    unicode-bidi: isolate;
    width: 100%;
    -webkit-font-smoothing: antialiased;
  }

  .pdp-featured-legacy__inner {
    width: calc(100% - 320px);
    max-width: 1520.8px;
    min-height: 816px;
    margin: 0 auto;
    padding: 36px 0;
  }

  .pdp-featured-legacy__grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: 32px;
    align-items: start;
  }

  .pdp-featured-legacy__media {
    border-radius: 12px;
    overflow: hidden;
    background: #f5f5f5;
  }

  .pdp-featured-legacy__media img {
    width: 100%;
    height: auto;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    display: block;
  }

  .pdp-featured-legacy__info {
    color: #1a1a1a;
  }

  .pdp-featured-legacy__title {
    margin: 0 0 16px;
    font-size: 22px;
    line-height: 1.25;
    font-weight: 700;
    font-family: 'Roboto Condensed', 'Arial Narrow', sans-serif;
    text-transform: uppercase;
  }

  .pdp-featured-legacy__info h5 {
    margin: 18px 0 6px;
    font-size: 16px;
    line-height: 1.4;
    font-weight: 700;
    font-family: 'Roboto Condensed', 'Arial Narrow', sans-serif;
  }

  .pdp-featured-legacy__info ul {
    margin: 0;
    padding-left: 18px;
  }

  .pdp-featured-legacy__info li {
    margin: 0 0 6px;
    font-size: 14px;
    line-height: 1.5;
    color: #2d2d2d;
    font-weight: 400;
  }

  .pdp-featured-legacy__link {
    display: inline-block;
    margin-top: 18px;
    color: #5510dd;
    text-decoration: none;
    font-weight: 600;
    border-bottom: 1px solid #5510dd;
  }

  .pdp-grid-banner {
    box-sizing: border-box;
    color: rgb(45, 45, 45);
    display: block;
    font-family: Inter, sans-serif;
    font-size: 14px;
    font-style: normal;
    font-weight: 300;
    line-height: 25.2px;
    margin: 0;
    padding: 0;
    unicode-bidi: isolate;
    width: 100%;
    -webkit-font-smoothing: antialiased;
    background: #f3f3f3;
  }

  .pdp-grid-banner__inner {
    width: calc(100% - 320px);
    max-width: 1520.8px;
    min-height: 608.938px;
    margin: 0 auto;
    padding: 80px 0 52px;
  }

  .pdp-grid-banner__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 30px;
  }

  .pdp-grid-banner__card {
    background: transparent;
    min-height: 400px;
    align-content: center;
  }

  .pdp-grid-banner__card--image {
    display: flex;
    align-items: center;
    justify-content: center;
    background: #f3f3f3;
    border-radius: 10px;
    overflow: hidden;
  }

  .pdp-grid-banner__card--image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    background: #f3f3f3;
  }

  .pdp-grid-banner__kicker {
    margin: 0 0 10px;
    color: #1b1b1b;
    font-size: 18px;
    line-height: 1.25;
    font-weight: 700;
    font-family: 'Roboto Condensed', 'Arial Narrow', sans-serif;
  }

  .pdp-grid-banner__headline {
    margin: 0 0 16px;
    color: #5510dd;
    font-size: 36px;
    line-height: 1.1;
    font-weight: 700;
    font-family: 'Roboto Condensed', 'Arial Narrow', sans-serif;
    text-transform: uppercase;
  }

  .pdp-grid-banner__description {
    margin: 0;
    font-size: 14px;
    line-height: 1.6;
    color: #2d2d2d;
    max-width: 420px;
  }

  .pdp-multicolumn {
    box-sizing: border-box;
    color: rgb(45, 45, 45);
    display: block;
    font-family: Inter, sans-serif;
    font-size: 14px;
    font-style: normal;
    font-weight: 300;
    line-height: 25.2px;
    margin: 0;
    padding: 0;
    unicode-bidi: isolate;
    width: 100%;
    -webkit-font-smoothing: antialiased;
    background: #fff;
  }

  .pdp-multicolumn__inner {
    width: calc(100% - 320px);
    max-width: 1520.8px;
    min-height: 854.162px;
    margin: 0 auto;
    padding: 100px 0 36px;
  }

  .pdp-multicolumn__header {
    text-align: center;
    margin-bottom: 30px;
  }

  .pdp-multicolumn__header h2 {
    margin: 0 0 14px;
    color: #5510dd;
    font-size: 22px;
    line-height: 1.2;
    font-weight: 800;
    font-family: 'Roboto Condensed', 'Arial Narrow', sans-serif;
    text-transform: uppercase;
  }

  .pdp-multicolumn__header p {
    width: 60%;
    margin: 0 auto;
    color: #212121;
    font-size: 14px;
    line-height: 1.6;
  }

  .pdp-multicolumn__list {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 30px;
    align-items: stretch;
  }

  .pdp-multicolumn__item {
    border-radius: 0;
    overflow: hidden;
    background: transparent;
  }

  .pdp-multicolumn__media {
    width: 100%;
    aspect-ratio: 16 / 9;
    background: #f5f5f5;
  }

  .pdp-multicolumn__media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .pdp-multicolumn__info {
    padding: 16px 0 0;
  }

  .pdp-multicolumn__info h6 {
    margin: 0;
    color: #1f1f1f;
    font-size: 17px;
    line-height: 1.45;
    font-weight: 400;
    font-family: Inter, sans-serif;
  }

  .pdp-multicolumn__dots {
    display: none;
  }

  @media screen and (max-width: 768px) {
    .pdp-social-proof__inner {
      width: calc(100% - 32px);
      padding: 32px 0 24px;
    }

    .pdp-social-proof__hero {
      gap: 32px;
      margin-bottom: 24px;
    }

    .pdp-social-proof__rating-block {
      min-width: 0;
      width: 100%;
    }

    .pdp-social-proof__rating-value {
      font-size: 48px;
    }

    .pdp-social-proof__stars {
      font-size: 28px;
    }

    .pdp-social-proof__people {
      width: 100%;
    }

    .pdp-social-proof__avatars span {
      width: 42px;
      height: 42px;
    }

    .pdp-social-proof__activity-bar {
      flex-direction: column;
      align-items: flex-start;
      gap: 16px;
      padding: 14px 16px;
    }

    .pdp-social-proof__delivery-pill {
      width: 100%;
      justify-content: center;
    }

    .pdp-social-proof__gallery-header {
      flex-direction: column;
      align-items: flex-start;
    }

    .pdp-social-proof__gallery-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 12px;
    }

    .pdp-social-proof__trust-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .pdp-social-proof__notification {
      left: 16px;
      right: 16px;
      bottom: 16px;
      max-width: none;
    }

    .pdp-social-proof__modal-close {
      top: -50px;
      right: 10px;
    }

    .pdp-benefits__inner {
      width: calc(100% - 32px);
      padding: 48px 0;
    }

    .pdp-benefits__header {
      margin-bottom: 40px;
    }

    .pdp-benefits__title {
      font-size: 36px;
    }

    .pdp-benefits__subtitle {
      font-size: 18px;
    }

    .pdp-featured-legacy__inner {
      width: calc(100% - 32px);
      min-height: 0;
      padding: 24px 0 16px;
    }

    .pdp-featured-legacy__grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .pdp-featured-legacy__title {
      font-size: 22px;
    }

    .pdp-grid-banner__inner {
      width: calc(100% - 32px);
      min-height: 0;
      padding: 60px 0 39px;
    }

    .pdp-grid-banner__grid {
      grid-template-columns: 1fr;
      gap: 15px;
    }

    .pdp-grid-banner__card {
      min-height: 200px;
    }

    .pdp-grid-banner__headline {
      font-size: 30px;
    }

    .pdp-multicolumn__inner {
      width: calc(100% - 32px);
      min-height: 0;
      padding: 75px 0 27px;
    }

    .pdp-multicolumn__header h2 {
      font-size: 22px;
    }

    .pdp-multicolumn__header p {
      width: 90%;
    }

    .pdp-multicolumn__list {
      display: flex;
      overflow-x: auto;
      overflow-y: hidden;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
      gap: 0;
      scrollbar-width: none;
    }

    .pdp-multicolumn__list::-webkit-scrollbar {
      display: none;
    }

    .pdp-multicolumn__item {
      flex: 0 0 100%;
      min-width: 100%;
      scroll-snap-align: start;
      padding-right: 0;
    }

    .pdp-multicolumn__media {
      width: 90%;
      margin: 0 auto;
      aspect-ratio: 16 / 9;
    }

    .pdp-multicolumn__info {
      width: 90%;
      margin: 0 auto;
      padding-top: 14px;
    }

    .pdp-multicolumn__dots {
      display: flex;
      justify-content: center;
      gap: 8px;
      margin-top: 14px;
    }

    .pdp-multicolumn__dot {
      width: 8px;
      height: 8px;
      border-radius: 999px;
      border: 0;
      padding: 0;
      background: #d4d4d4;
      cursor: pointer;
    }

    .pdp-multicolumn__dot[aria-current='true'] {
      width: 20px;
      background: #5510dd;
    }

    .pdp-multicolumn__dot:focus-visible {
      outline: 2px solid #5510dd;
      outline-offset: 2px;
    }
  }

  @media screen and (min-width: 768px) {
    .pdp-benefits__grid {
      grid-template-columns: repeat(3, 413px);
      justify-content: center;
      gap: 20px;
    }

    .pdp-benefits__card {
      width: 413px;
      min-height: 327px;
    }

    .pdp-benefits__card-content {
      padding: 18px;
    }

    .pdp-benefits__title {
      font-size: 48px;
    }

    .pdp-grid-banner__card {
      min-height: 300px;
    }
  }
{% endstylesheet %}

<script>
  (() => {
    const customers = [
      { name: 'Carlos R.', location: 'Madrid', product: 'Athlete Bundle' },
      { name: 'Sarah M.', location: 'London', product: 'Ascent MAX Bundle' },
      { name: 'Anna K.', location: 'Berlin', product: 'Athlete Bundle' },
      { name: 'James L.', location: 'Sydney', product: 'Ascent Bundle' },
      { name: 'Emma S.', location: 'Toronto', product: 'Ascent MAX Bundle' },
    ];

    const initSocialProof = (root) => {
      if (!root || root.dataset.initialized === 'true') return;
      root.dataset.initialized = 'true';

      const viewerCount = root.querySelector('[data-viewer-count]');
      const countdown = root.querySelector('[data-countdown]');
      const shippingTitle = root.querySelector('[data-shipping-title]');
      const shippingDescription = root.querySelector('[data-shipping-description]');
      const modal = root.querySelector('[data-video-modal]');
      const videoFrame = root.querySelector('[data-video-frame]');
      const notification = root.querySelector('[data-notification]');
      const notificationImage = root.querySelector('[data-notification-image]');
      const notificationName = root.querySelector('[data-notification-name]');
      const notificationAction = root.querySelector('[data-notification-action]');
      const notificationTime = root.querySelector('[data-notification-time]');
      const notificationClose = root.querySelector('[data-notification-close]');
      const imageSrc = root.dataset.productImage;
      let customerIndex = 0;

      const updateViewerCount = () => {
        if (!viewerCount) return;
        viewerCount.textContent = String(Math.floor(Math.random() * 30) + 35);
      };

      const updateCountdown = () => {
        if (!countdown) return;
        const now = new Date();
        const cutoff = new Date(now);
        cutoff.setHours(21, 0, 0, 0);

        if (now >= cutoff) {
          cutoff.setDate(cutoff.getDate() + 1);
        }

        const diff = cutoff - now;
        const hours = Math.floor(diff / (1000 * 60 * 60));
        const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
        countdown.textContent = `${hours}h ${minutes}m`;
      };

      const updateShippingInfo = () => {
        if (!shippingTitle || !shippingDescription) return;
        const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone || '';
        const locale = (navigator.language || '').toLowerCase();

        if (timezone.includes('Europe') || ['de', 'fr', 'it', 'es', 'nl', 'be', 'at', 'pt', 'ie', 'se', 'dk', 'fi', 'no'].some((code) => locale.startsWith(code))) {
          shippingTitle.textContent = 'Free Shipping Over €50';
          shippingDescription.textContent = 'Fast delivery to EU, UK, US, Canada and Australia. Worldwide available.';
          return;
        }

        if (timezone.includes('London') || locale.startsWith('en-gb')) {
          shippingTitle.textContent = 'Free Shipping Over £42.50';
          shippingDescription.textContent = 'Fast delivery to EU, UK, US, Canada and Australia. Worldwide available.';
          return;
        }

        if (timezone.includes('America') || locale.startsWith('en-us')) {
          shippingTitle.textContent = 'Free Shipping Over $54';
          shippingDescription.textContent = 'Fast delivery to EU, UK, US, Canada and Australia. Worldwide available.';
          return;
        }

        shippingTitle.textContent = 'Worldwide Shipping Available';
        shippingDescription.textContent = 'Fast international delivery. Free shipping available in select regions.';
      };

      const showNotification = () => {
        if (!notification) return;
        const customer = customers[customerIndex % customers.length];
        customerIndex += 1;
        if (notificationImage && imageSrc) notificationImage.src = imageSrc;
        if (notificationName) notificationName.textContent = `${customer.name} from ${customer.location}`;
        if (notificationAction) notificationAction.textContent = `just purchased ${customer.product}`;
        if (notificationTime) notificationTime.textContent = `${Math.floor(Math.random() * 5) + 1} minutes ago`;
        notification.hidden = false;
        window.setTimeout(() => {
          notification.hidden = true;
        }, 5000);
      };

      root.querySelectorAll('[data-video-id]').forEach((button) => {
        button.addEventListener('click', () => {
          if (!modal || !videoFrame) return;
          const videoId = button.getAttribute('data-video-id');
          videoFrame.src = `https://www.youtube.com/embed/${videoId}?autoplay=1`;
          modal.hidden = false;
          document.body.style.overflow = 'hidden';
        });
      });

      root.querySelector('[data-close-video]')?.addEventListener('click', () => {
        if (!modal || !videoFrame) return;
        modal.hidden = true;
        videoFrame.src = '';
        document.body.style.overflow = '';
      });

      modal?.addEventListener('click', (event) => {
        if (event.target !== modal || !videoFrame) return;
        modal.hidden = true;
        videoFrame.src = '';
        document.body.style.overflow = '';
      });

      notificationClose?.addEventListener('click', () => {
        if (notification) notification.hidden = true;
      });

      updateViewerCount();
      updateCountdown();
      updateShippingInfo();
      window.setInterval(updateViewerCount, 8000);
      window.setInterval(updateCountdown, 60000);
      window.setTimeout(showNotification, 3000);
      window.setInterval(showNotification, 30000);
    };

    const initMulticolumnCarousel = (root) => {
      if (!root || root.dataset.carouselInitialized === 'true') return;

      const list = root.querySelector('.pdp-multicolumn__list');
      const slides = list ? Array.from(list.querySelectorAll('.pdp-multicolumn__item')) : [];
      const dotsContainer = root.querySelector('[data-multi-dots]');

      if (!list || slides.length < 2 || !dotsContainer) return;

      root.dataset.carouselInitialized = 'true';

      let currentIndex = 0;
      let autoplayId = null;

      const isMobile = () => window.matchMedia('(max-width: 768px)').matches;

      const renderDots = () => {
        dotsContainer.innerHTML = '';
        slides.forEach((_, index) => {
          const dot = document.createElement('button');
          dot.type = 'button';
          dot.className = 'pdp-multicolumn__dot';
          dot.setAttribute('aria-label', `Ir a slide ${index + 1}`);
          dot.setAttribute('aria-current', index === currentIndex ? 'true' : 'false');
          dot.addEventListener('click', () => {
            goToSlide(index);
            restartAutoplay();
          });
          dotsContainer.appendChild(dot);
        });
      };

      const updateDots = () => {
        const dots = dotsContainer.querySelectorAll('.pdp-multicolumn__dot');
        dots.forEach((dot, index) => {
          dot.setAttribute('aria-current', index === currentIndex ? 'true' : 'false');
        });
      };

      const goToSlide = (index, behavior = 'smooth') => {
        currentIndex = (index + slides.length) % slides.length;
        if (isMobile()) {
          const target = slides[currentIndex];
          list.scrollTo({ left: target.offsetLeft, behavior });
        }
        updateDots();
      };

      const syncIndexFromScroll = () => {
        if (!isMobile()) return;
        const center = list.scrollLeft + list.clientWidth / 2;
        let closestIndex = 0;
        let closestDistance = Number.POSITIVE_INFINITY;

        slides.forEach((slide, index) => {
          const slideCenter = slide.offsetLeft + slide.clientWidth / 2;
          const distance = Math.abs(center - slideCenter);
          if (distance < closestDistance) {
            closestDistance = distance;
            closestIndex = index;
          }
        });

        if (closestIndex !== currentIndex) {
          currentIndex = closestIndex;
          updateDots();
        }
      };

      const stopAutoplay = () => {
        if (autoplayId) {
          window.clearInterval(autoplayId);
          autoplayId = null;
        }
      };

      const startAutoplay = () => {
        stopAutoplay();
        if (!isMobile()) return;
        autoplayId = window.setInterval(() => {
          goToSlide(currentIndex + 1);
        }, 4500);
      };

      const restartAutoplay = () => {
        stopAutoplay();
        startAutoplay();
      };

      renderDots();
      goToSlide(0, 'auto');
      startAutoplay();

      list.addEventListener('scroll', syncIndexFromScroll, { passive: true });
      list.addEventListener('pointerdown', stopAutoplay, { passive: true });
      list.addEventListener('pointerup', startAutoplay, { passive: true });
      window.addEventListener('resize', () => {
        goToSlide(currentIndex, 'auto');
        restartAutoplay();
      });
    };

    const initAll = () => {
      document.querySelectorAll('[data-social-proof]').forEach(initSocialProof);
      document.querySelectorAll('[data-pdp-multicolumn]').forEach(initMulticolumnCarousel);
    };

    initAll();
    document.addEventListener('shopify:section:load', initAll);
    document.addEventListener('keydown', (event) => {
      if (event.key !== 'Escape') return;
      document.querySelectorAll('[data-video-modal]').forEach((modal) => {
        const iframe = modal.querySelector('[data-video-frame]');
        modal.hidden = true;
        if (iframe) iframe.src = '';
        document.body.style.overflow = '';
      });
    });
  })();
</script>
